data-sources:
  dataSources:
    - name: data-base-1
      url: jdbc:postgresql://localhost:5433/db1
      user: testuser
      password: testpass
      table: users
      mapping:
        id: user_id
        username: login
        name: first_name
        surname: last_name

    - name: data-base-2
      url: jdbc:postgresql://localhost:5434/db2
      user: testuser
      password: testpass
      table: user_table
      mapping:
        id: ldap_login
        username: ldap_login
        name: name
        surname: surname